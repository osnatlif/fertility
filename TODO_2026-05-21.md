# Tomorrow's TODO — improve fit before estimation

End-of-day 2026-05-20 state: **objective = 2217.10** (from 9812 at start of session).

## Current biggest moment contributions

| Moment | Value | Notes |
|--------|------:|-------|
| **Wage husband single** | **634.68** | Largest unaddressed regression. Hasn't been investigated. |
| **Childless unmarried** | 530.04 | Some HS women still over-fertile at older ages (pathway-1 divorcees with kids) |
| **Childless married** | 377.94 | Young HS married moms still have too few kids; partly fertility-rate-late bias |
| Marriage men | 325.94 | |
| Marriage women | 254.22 | |
| Assortative women | 145.55 | SC-CG and HS-CG cells still ~0 in model vs 0.30 and 0.12 in data |

## Priority 1 — fit improvements

### (a) Investigate `Wage husband single` (634)
**Largest single moment.** Hasn't been examined since wife_exp was added. Likely culprits:
- Selection effect: with cb_const + tuning, the pool of "single husbands" shifted toward a different sub-population than data
- Husband wage equation parameters (`beta31_h, beta32_h, beta33_h`) calibrated against a different selection
- Possibly wage-by-edu structure for unmarried men differs from married

Action: read `===== SINGLE MEN =====` moment table, identify which edu × age cells are most off, then targeted parameter check.

### (b) Cross-edu marriage gap (SC×CG, HS×CG)
Model: SC-CG = 0.03 vs data 0.30. HS-CG = 0.01 vs data 0.12. Other off-diagonals OK.

The constraint: CG men prefer CG women so strongly that non-CG women can't get them. Symmetric mconst1 can't fix this without breaking diagonal preferences.

Options:
- Add asymmetric "marrying up" bonus (woman with lower edu than husband gets a bonus)
- Reduce taste_cg slightly (currently 400) and accept less CG-CG to gain SC-CG
- Edu-conditional matching probabilities (Osnat ruled out earlier — revisit only if other approaches fail)

### (c) Fertility moments
- **HS young married moms too few kids**: model 0.60 vs data 1.33 at HS 22-26. Despite initial conditions, the endogenous fertility decision still delays.
  - Possible: bump `p.preg_married` (currently 0) slightly positive — direct period-utility for being pregnant when married
  - Or: bump `alpha3_w_m` back up modestly (currently 27, was 30) and re-check
  - Or: reduce `childcare_cost` (currently $5000 — could be lower for the 1960 cohort era)
- **HS unmarried late-age fertility**: 2.46 vs data 1.64. These are pathway-1 (had kids while married, then divorced). Could try reducing divorce among moms or accept (pathway-1 issue is hard to fix without per-state divorce cost).

### (d) Robustness check on initial conditions
We promised in the appendix that small variations in `f_start` (currently 14) don't materially change the entry distributions. Quick test: rerun with `f_start ∈ {12, 16}`, compare objective. Updates the appendix if confirmed (or revises if not).

## Priority 2 — extension to other cohorts

### (e) Test 1985 cohort
- Drop in `input/initial_kids_1985white.txt` (need to compute the probabilities from 1985 data — Osnat to provide age-18, age-20 distributions if available)
- Run with same preference parameters, see how fit compares
- Confirms (or refutes) the "universal preferences + cohort initials" identification strategy

## Priority 3 — set up estimation framework

Once fit is "good enough" (let's say objective < 2000), move to formal estimation:

### (f) Pick parameters to estimate
Candidates (the ones we've been tuning by hand):
- `alpha3_w_m`, `alpha3_w_s` (kid utility married/single)
- `taste_hs`, `taste_sc`, `taste_cg` (same-edu marriage premium)
- `mconst1`, `mconst2` (off-diagonal marriage)
- `dc_w`, `dc_h` (divorce costs)
- `t5_w`, `t5_h`, `t6_w`, `t6_h` (terminal values)
- `beta31_w`, `beta32_w`, `beta33_w`, `beta13_w` (wife wage constants/slopes)
- `lambda2_w_ft` (wife FT offer education effect)

Fix wage equation slopes (beta11-23) and most husband-side params for now.

### (g) Set up bounds and starting values
Use current parameter values as the starting point.

### (h) Pick optimizer
Options:
- **Nelder-Mead** (derivative-free, simple, slow but robust)
- **Powell** (similar, sometimes faster)
- **Custom random search + local refinement**

Given backward solve is now ~155s (with wife_exp + N=3,3 GH), each function eval takes ~3 min. A typical optimizer needs 100-500 evals → 5-25 hours. Tractable overnight but not interactive.

### (i) Weighting matrix
Currently objective is unweighted sum of MSE components. May want to weight by inverse variance of empirical moments for formal asymptotic results.

## Notes / state at end of day

- `run_mconst_90_50_1960white.log` is the best result (2217).
- All current code state reflects this run (no parameter changes pending).
- Memory updated with all key design choices.
- `appendix_initial_conditions.tex` is finished, may need data-source citation filled in.
- The diagnostic stay-home counters are still in `forward_simulation.py` — can be removed when no longer needed.

## Open questions for tomorrow

1. Do we have 1985 cohort age-18/age-20 fertility data ready?
2. What's the data source name for the appendix citation?
3. Do you want to investigate Wage husband single before (b)/(c), or in parallel?
